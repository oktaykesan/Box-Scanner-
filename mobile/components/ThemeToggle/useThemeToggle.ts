// Re-exports animation logic — the actual state machine lives in index.tsx
// This file exists for the component structure spec compliance.
// Use `useTheme()` from contexts/ThemeContext for theme access.
export { useTheme } from '../../contexts/ThemeContext';
